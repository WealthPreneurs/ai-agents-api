# Agent photos

Drop a headshot here named `<agent-id>.jpg` to give that agent a real photo
on the Your Team page. Agent ids are defined in `src/data/agents.js`.

Currently expected:
- `xavier-blake.jpg` — Social Media Manager

If a file is missing, the Team page automatically falls back to a styled
initials badge, so this folder can stay empty without breaking anything.
